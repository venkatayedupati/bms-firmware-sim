name: CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Build simulator
        run: make sim
      - name: Run unit tests
        run: make test

  sanitize:
    # Linux only: TSan/ASan interceptor behavior for pthreads is most mature
    # there, and one consistent platform is enough to get sanitizer signal
    # without doubling flaky-CI surface area from a second OS's libc/runtime
    # quirks.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ThreadSanitizer + AddressSanitizer/UBSan (sim + tests)
        run: make sanitize

  static-analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install clang-tidy and cppcheck
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang-tidy cppcheck
      - name: clang-tidy
        run: make tidy CLANG_TIDY=clang-tidy
      - name: cppcheck
        run: make cppcheck-run
      # can_hal_socketcan.c isn't in SIM_SRCS (it's Linux-only, so `make
      # tidy`/`cppcheck-run` stay usable on macOS too), but this job is
      # always Linux, so it's the one place able to statically check it.
      - name: clang-tidy (SocketCAN backend, Linux-only file)
        run: clang-tidy src/can/can_hal_socketcan.c -- -std=c11 -Isrc
      - name: cppcheck (SocketCAN backend, Linux-only file)
        run: |
          cppcheck --enable=warning,performance,portability,style --std=c11 -Isrc \
            --suppress=missingIncludeSystem --inline-suppr --error-exitcode=1 \
            src/can/can_hal_socketcan.c
      # fuzz/fuzz_can_protocol.c isn't in SIM_SRCS/TEST_SRCS either (it only
      # compiles with a libFuzzer-capable clang), same reasoning as the
      # SocketCAN file above.
      - name: clang-tidy (fuzz harness)
        run: clang-tidy fuzz/fuzz_can_protocol.c -- -std=c11 -Isrc
      - name: cppcheck (fuzz harness)
        run: |
          cppcheck --enable=warning,performance,portability,style --std=c11 -Isrc \
            --suppress=missingIncludeSystem --inline-suppr --error-exitcode=1 \
            fuzz/fuzz_can_protocol.c
      - name: clang-tidy (DBC verification probe)
        run: clang-tidy dbc/verify_dbc.c -- -std=c11 -Isrc -Isrc/can
      - name: cppcheck (DBC verification probe)
        run: |
          cppcheck --enable=warning,performance,portability,style --std=c11 -Isrc -Isrc/can \
            --suppress=missingIncludeSystem --inline-suppr --error-exitcode=1 \
            dbc/verify_dbc.c

  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Ubuntu 24.04's apt package is lcov 2.0-1, which has a real rendering
      # bug in `lcov --list`'s function-coverage percentage column (verified
      # directly: the underlying .info data's FNH/FNF counts were already
      # correct -- e.g. 6 hit / 6 found -- while --list rendered that same
      # data as "900%"). Installing 2.5 from source instead, matching the
      # Homebrew version already verified correct in local development.
      - name: Install lcov 2.5 from source (Ubuntu's apt lcov 2.0-1 has a known --list percentage bug)
        # Copying bin/+lib/ and wrapping with `perl -I lib` instead of
        # `make install`: the packaged install target also builds man pages
        # via sphinx-build (not present on the runner, not needed here).
        # These are lcov 2.x's actual (non-core-Perl) CPAN dependencies --
        # apt has them prepackaged as Debian modules (confirmed via
        # `apt-cache depends lcov`, since lcov's own release tarball
        # doesn't declare them anywhere convenient to introspect).
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq \
            libcapture-tiny-perl libdatetime-perl libtimedate-perl \
            libjson-perl libperlio-gzip-perl
          curl -sL https://github.com/linux-test-project/lcov/releases/download/v2.5/lcov-2.5.tar.gz -o /tmp/lcov.tar.gz
          tar xzf /tmp/lcov.tar.gz -C /tmp
          sudo mkdir -p /usr/local/libexec/lcov
          sudo cp -r /tmp/lcov-2.5/bin /tmp/lcov-2.5/lib /usr/local/libexec/lcov/
          for f in lcov geninfo genhtml gendesc; do
            printf '#!/bin/bash\nexec perl -I/usr/local/libexec/lcov/lib /usr/local/libexec/lcov/bin/%s "$@"\n' "$f" \
              | sudo tee /usr/local/bin/$f > /dev/null
            sudo chmod +x /usr/local/bin/$f
          done
          lcov --version
      - name: Run tests with coverage instrumentation and report
        run: make coverage

  dbc-verify:
    # dbc/bms.dbc is a real cantools/Vector CANoe-loadable DBC hand-written
    # from docs/CAN_PROTOCOL.md's spec. Motorola (big-endian) DBC start-bit
    # arithmetic is easy to get subtly wrong by hand, so this doesn't just
    # trust it -- dbc/verify_dbc.c packs known values through this
    # project's actual can_protocol_pack_* functions, and dbc/verify_dbc.py
    # decodes the resulting real wire bytes via cantools and checks them
    # against those same known values.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Ubuntu 24.04's system Python blocks a bare `pip install` (PEP 668,
      # "externally-managed-environment") -- verified directly -- hence the
      # venv instead of installing cantools globally.
      - name: Install cantools into a venv
        run: |
          python3 -m venv /tmp/dbc-venv
          /tmp/dbc-venv/bin/pip install -q cantools
          echo "/tmp/dbc-venv/bin" >> "$GITHUB_PATH"
      - name: Verify dbc/bms.dbc decodes real can_protocol.c output correctly
        run: make dbc-verify PYTHON=/tmp/dbc-venv/bin/python3

  fuzz:
    # can_protocol.c's unpack_* functions are the one place in this codebase
    # that parses attacker/peer-controlled bytes (whatever id/dlc/data a
    # real bus frame carried) -- see fuzz/fuzz_can_protocol.c. Ubuntu's
    # plain `clang` apt package bundles libFuzzer's compiler-rt runtime
    # fine (verified directly); Apple's Command Line Tools clang on macOS
    # does not, which is why FUZZ_CC defaults to Homebrew's LLVM there
    # instead -- this job overrides it back to plain `clang` on this
    # runner. 120s is a fixed CI budget, not a real fuzzing campaign length.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install clang
        run: sudo apt-get update -qq && sudo apt-get install -y -qq clang
      - name: Fuzz can_protocol.c's unpack functions (120s budget)
        run: make fuzz-run FUZZ_CC=clang FUZZ_TIME=120

  socketcan-build:
    # Compile/link verification only. Live functional testing against a
    # real vcan0 needs a genuine Linux machine or full VM -- verified
    # directly that neither Docker Desktop nor GitHub's hosted Linux
    # runner can provide one: Docker Desktop's kernel lacks the vcan
    # module outright (`modprobe vcan`: "Module vcan not found"), and
    # GitHub's hosted runner's kernel lacks it too, in a way that isn't
    # even a loadable-module gap -- `ip link add dev vcan0 type vcan`
    # itself fails there with "Unknown device type". See
    # docs/ARCHITECTURE.md "Portability" for how to test this yourself
    # against a real vcan0 if you have an environment that has one.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build the SocketCAN-backed simulator (compiles + links against real Linux CAN headers)
        run: make sim-socketcan

  freertos-qemu:
    # Unlike vcan0, QEMU's system emulation needs no special host kernel
    # module -- it's a userspace program -- so this job verifies actual
    # behavior, not just compilation: it boots the real cross-compiled ELF
    # under QEMU's mps2-an385 (Cortex-M3) machine and confirms the same
    # task startup sequence the host build produces, running on a real
    # FreeRTOS kernel (vendored at third_party/FreeRTOS-Kernel) instead of
    # pthreads. See src/osal/osal_freertos.c and docs/ARCHITECTURE.md
    # "Portability".
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install qemu-system-arm
        run: sudo apt-get update -qq && sudo apt-get install -y -qq qemu-system-arm
      # Most distro/Homebrew arm-none-eabi-gcc packages are bare compilers
      # with no bundled libc (confirmed directly: Homebrew's is built
      # --without-headers); this is the official ARM release, which
      # bundles newlib including newlib-nano's nano/nosys specs.
      - name: Install the ARM GNU Toolchain (bundles newlib)
        run: |
          curl -sL "https://gitlab.arm.com/api/v4/projects/tooling%2Fgnu-toolchains-for-arm/packages/generic/gnu-toolchain/15.3.rel1/arm-gnu-toolchain-15.3.rel1-x86_64-arm-none-eabi.tar.xz" -o /tmp/toolchain.tar.xz
          mkdir -p /tmp/arm-toolchain
          tar xJf /tmp/toolchain.tar.xz -C /tmp/arm-toolchain --strip-components=1
          echo "/tmp/arm-toolchain/bin" >> "$GITHUB_PATH"
      - name: Build for QEMU mps2-an385
        run: make freertos-build
      - name: Boot under QEMU and verify the real FreeRTOS kernel runs this project's actual tasks
        run: |
          # 10s, not 5s: stack_report.c's one-shot report only fires 3s
          # (simulated) after boot, and real wall-clock time to reach that
          # point depends on this runner's QEMU emulation speed, not just
          # the simulated delay.
          timeout 10 qemu-system-arm -M mps2-an385 -nographic \
            -kernel targets/qemu_mps2_an385/build/bms_sim.elf > /tmp/qemu.log 2>&1 || true
          cat /tmp/qemu.log
          for t in sensor soc fault can watchdog; do
            grep -q "$t .*task started" /tmp/qemu.log
          done
          # Confirms stack_report.c's real per-task stack high-water-mark
          # report actually printed -- see docs/ARCHITECTURE.md "Memory
          # footprint" -- not just that the sim booted.
          grep -q "Stack high-water marks" /tmp/qemu.log
          for t in sensor soc fault can watchdog stack_report IDLE; do
            grep -q "^$t" /tmp/qemu.log
          done
